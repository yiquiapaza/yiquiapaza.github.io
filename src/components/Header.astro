---
import Toggle from '../components/ThemeChange.astro';
const pathName = new URL(Astro.request.url);
const currentPath = pathName.pathname.slice(1);
---

<div class="container mx-auto max-w-6xl px-6 py-16 md:py-16">
    <nav class="container relative mx-auto">
        <div
            class="flex flex-col items-center md:items-baseline space-x-2.5 md:flex-row md:space-x-20"
        >
            <h1 class="text-4xl md:text-5xl object-left">Yhonatan</h1>
            <div class=" object-right space-x-4 py-10 text-base md:w-full md:p-0 md:text-2xl">
                <a
                    href="/"
                    class:list={[
                        'font-poppinsRegular',
                        currentPath === ''
                            ? 'font-bold text-accessible-text-blue-2 underline underline-offset-8 dark:text-dark-accessible-text-blue-2'
                            : '',
                    ]}
                >
                    Home</a
                >
                <a
                    href="/publications"
                    class:list={[
                        'font-poppinsRegular',
                        currentPath === 'publications'
                            ? 'font-bold text-accessible-text-blue-2 underline underline-offset-8 dark:text-dark-accessible-text-blue-2'
                            : '',
                    ]}
                >
                    Publications</a
                >
                <a
                    href="/projects"
                    class:list={[
                        'font-poppinsRegular',
                        currentPath === 'projects'
                            ? 'font-bold text-accessible-text-blue-2 underline underline-offset-8 dark:text-dark-accessible-text-blue-2'
                            : '',
                    ]}
                >
                    Projects</a
                >
                <!--TODO: change personal drive resume url-->
                <a
                    href="/resume"
                    class:list={[
                        'font-poppinsRegular',
                        currentPath === 'resume'
                            ? 'font-bold text-accessible-text-blue-2 underline underline-offset-8 dark:text-dark-accessible-text-blue-2'
                            : '',
                    ]}
                >
                    Resume</a
                >
            </div>
        </div>
    </nav>
</div>
<script>
    const headerNames: NodeListOf<HTMLAnchorElement> =
        document.querySelectorAll('nav div div a');
    headerNames.forEach((headerName) => {
        headerName.classList.remove('active');
        const currentPath = window.location.pathname.slice(1);
        const hrefArray = headerName.href.split('/');
        const thisPath = hrefArray[hrefArray.length - 1];
        if (currentPath === thisPath) {
            headerName.classList.add('active');
        }
    });
</script>
